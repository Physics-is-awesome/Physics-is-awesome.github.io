/**
 * YouTube channel + videos.
 *
 * Set CHANNEL_URL once you have it — the "Subscribe" button on the
 * YouTube page only appears when this is non-empty.
 *
 * For each video: set `url` to the real YouTube link when published.
 * Leave `thumbnail` as "" to use the built-in placeholder graphic —
 * or point it at an image path (e.g. "assets/img/thumb-01.jpg") once
 * you export a real thumbnail.
 */
const CHANNEL_URL = "";

const VIDEOS = [
  {
    title: "Can You Destroy a Universe with a Force?",
    description:
      "A visual, first-principles look at what it would actually take — " +
      "and what breaks first — if you tried to tear a universe apart with " +
      "a single applied force.",
    url: "",
    thumbnail: "",
    date: "",
  },
];
